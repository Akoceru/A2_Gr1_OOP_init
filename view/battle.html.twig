{% extends 'base.html.twig' %}
{% block title %}
    {{ parent() }} - Supaaa
{% endblock %}
{% block content %}
    <h1>Wellcome for the amazing Pokemon Online tournament!</h1>
        <h2>Welcome {{ username }}  </h2>
    <table class="table table-responsive"><thead><th>username</th> <th>Pokemon</th></thead>
        <tbody>
        {% for user in users %}
            {% if user.havepokemon == 1 %}
                {% if user.username == session.username %}

                {% else %}
                    <tr><td>{{ user.username }} </td> <td>{{ user.pokemonname }} <a href="attack.php?id={{ user.id }}">Attack!</a></td></tr>
                    {% endif %}
            {% else %}

            {% endif %}
        {% endfor %}
        </tbody>
    </table>

{% endblock %}